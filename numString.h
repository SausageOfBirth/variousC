#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <strings.h>

char* numbers[32];

int maxLen;

char* concat(char* str1,char* str2);
char* intToString(int num);
int lenstr(char* str, int max);
